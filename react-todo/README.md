# React Todo (with Jest + React Testing Library)

- Vite + React app
- TodoList + AddTodoForm components
- Tests in `src/__tests__/TodoList.test.jsx`

## Scripts
- `npm run dev` — start Vite dev server
- `npm test` — run Jest tests
